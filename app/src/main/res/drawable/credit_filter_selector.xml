<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Палец нажимает на пункт, но пункт ещё не активирован -->
  <item android:state_pressed="true">
    <ripple android:color="@color/diagram_filter_pressed_color" />
  </item>

  <!-- Пункт активирован.  В режиме SINGLE_CHOICE_MODE пункт подсвечивается -->
  <item android:state_activated="true"
      android:drawable="@color/diagram_filter_checked_color" />

  <!-- По умолчанию -->
  <item android:drawable="@color/diagram_filter_unchecked_color" />
</selector>